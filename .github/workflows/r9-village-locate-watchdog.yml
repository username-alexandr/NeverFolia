name: R9 Village Locate Watchdog

# Isolated R9 release-blocker gate; kept off the candidate branch until proven green.
on:
  push:
    branches:
      - qa/r9-village-locate-watchdog
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: r9-village-locate-watchdog-${{ github.ref }}
  cancel-in-progress: true

jobs:
  village-locate:
    name: All villages + cartographer tags
    runs-on: ubuntu-latest
    timeout-minutes: 90

    steps:
      - name: Checkout NeverFolia
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Set up JDK 25
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '25'

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v6

      - name: Configure Git identity
        run: |
          git config --global user.email "actions@github.com"
          git config --global user.name "NeverFolia CI"

      - name: Prepare Folia 26.2.x upstream
        run: |
          chmod +x ./scripts/prepare-upstream.sh
          ./scripts/prepare-upstream.sh

      - name: Apply Folia patches
        run: |
          chmod +x ./scripts/apply-folia-patches-ci.sh
          ./scripts/apply-folia-patches-ci.sh ./.work/Folia

      - name: Apply NeverFolia post patches
        run: |
          chmod +x ./scripts/apply-neverfolia-post-patches.sh
          ./scripts/apply-neverfolia-post-patches.sh

      - name: Build runnable Paperclip JAR
        working-directory: .work/Folia
        env:
          BUILD_NUMBER: ${{ github.run_number }}
        run: ./gradlew :folia-server:createPaperclipJar --stacktrace

      - name: Collect JAR and build NeverOverworld pack
        run: |
          set -euo pipefail
          mkdir -p artifacts
          JAR="$(find .work/Folia/folia-server/build/libs -maxdepth 1 -type f -name '*.jar' ! -name '*sources.jar' ! -name '*javadoc.jar' | sort | head -n 1)"
          if [ -z "${JAR}" ]; then
            echo 'Runnable server JAR not found.' >&2
            exit 1
          fi
          cp "${JAR}" artifacts/NeverFolia-r9-village-locate.jar
          PACK="artifacts/NeverOverworld-Core-NR-DEV-1-native-structures-v1.zip"
          python3 ./scripts/build-never-overworld-core-pack.py \
            --server-jar artifacts/NeverFolia-r9-village-locate.jar \
            --output "${PACK}"
          python3 ./scripts/build-never-overworld-native-structures.py --input "${PACK}"
          python3 ./scripts/normalize-never-overworld-structure-type.py --input "${PACK}"
          python3 ./scripts/fingerprint-never-overworld-pack.py --input "${PACK}" --inject
          python3 ./scripts/fingerprint-never-overworld-pack.py --input "${PACK}" --verify

      - name: Run all-village bounded locate gate
        run: |
          chmod +x ./scripts/probe-never-overworld-village-locate-r9.sh
          ./scripts/probe-never-overworld-village-locate-r9.sh \
            artifacts/NeverFolia-r9-village-locate.jar \
            artifacts/NeverOverworld-Core-NR-DEV-1-native-structures-v1.zip

      - name: Upload village locate diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: R9-village-locate-${{ github.sha }}
          path: |
            artifacts/village-locate-r9-proof.json
            overworld-village-locate-r9-test/results.tsv
            overworld-village-locate-r9-test/segment-*.log
            overworld-village-locate-r9-test/server.log
            overworld-village-locate-r9-test/chunks-before-locate.txt
            overworld-village-locate-r9-test/chunks-after-locate.txt
          if-no-files-found: warn
          retention-days: 14
