name: R9 Village Variant Mask V16

on:
  push:
    branches:
      - qa/r9-village-variant-mask-v16

permissions:
  contents: read

concurrency:
  group: r9-village-variant-mask-v16-${{ github.ref }}
  cancel-in-progress: true

jobs:
  obsolete:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "V16 superseded by hardened V16b"
