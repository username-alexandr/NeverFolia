name: R9 Village Variant Mask V16b

on:
  push:
    branches:
      - qa/r9-village-variant-mask-v16b

permissions:
  contents: read

concurrency:
  group: r9-village-variant-mask-v16b-${{ github.ref }}
  cancel-in-progress: true

jobs:
  obsolete:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "V16b superseded by V17 reclamation"
